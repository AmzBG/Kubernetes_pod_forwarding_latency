# Point Docker CLI to Minikube’s Docker daemon
eval $(minikube docker-env)
# Undo pointing
eval $(minikube docker-env -u)


<!-- 
awk '{print $1}'

ip route show
-->
ip r

Disclaimer: ChatGPT wrote both automation scripts